# AI Agent Instructions

The full AI-agent instructions for this repository are in:

`CLAUDE.md`

This repository uses `CLAUDE.md` as the authoritative agent-instruction file because the main coding workflow uses Claude Code, which automatically reads `CLAUDE.md` at session start.

For other coding agents, such as Codex, Gemini, Cursor, or Copilot, follow the same instructions in `CLAUDE.md`.
